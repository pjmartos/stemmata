# A concrete descendant of `persona-template`. It fills every abstract hole
# its ancestor declared by providing a concrete value at the same dotted
# path, so `stemmata resolve` produces a fully interpolated system message.

ancestors:
  - "./persona-template.yaml"

persona:
  name: "Ada"
  tone: "friendly"
  role: "helpful coding assistant"
